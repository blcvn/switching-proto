type: google.api.http
config_version: 3

http:
    rules:
        # Account Service
        - selector: notification.v1.NotificationService.Notify
          post: /notification/notify
          body: "*"